doctype html

paper-fab#add_post(icon="add", ui-sref="events.new")

div.row
  div.col-md-6.col-md-offset-3
    .page-header
      h1= __('home.views.home')

    div(ng-repeat="event in events")
      span(style="font-size:20px;")
        a(ui-sref='events.show({id: event._id})') {{event.title}}